// idée pour le chech admin

const flexSwitchCheckDefault1 = (user,isAdmin) => {
    let checkOn = isAdmin;
    
    if (checkOn) {
        user.isAdmin
    }else

};

ifVerified = function (a, b, opts) {
    if (a != b) {
        return true;
    } else {
        return false;
    }
}

// savegarder les data du check dans la BDD
// 
